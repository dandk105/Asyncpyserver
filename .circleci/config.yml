version: 2.1

jobs:
  build-and-test:
    docker:
        - image: circleci/python:3.6.4
    steps:
        - checkout
        - run:
            command: |
              python -m venv .
              pip install requirements.txt
              pytest

workflows:
  jobs:
    - build-and-test